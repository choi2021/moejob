import styled from 'styled-components';

const Wrapper = styled.ul`
  width: 100%;
  padding: 1rem;
  h3 {
    margin-bottom: 0.5rem;
    font-weight: bold;
  }
`;

const S = {
  Wrapper,
};

export default S;
